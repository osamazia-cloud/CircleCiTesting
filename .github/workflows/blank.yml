# This is a basic workflow to help you get started with Actions    

name: CI

on:
#   push:
#     branches:
#       - master
  pull_request:
    branches: [ master ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - name: Try to fail
        if: ${{ secrets.RUN_CI_DEPLOY == 'false' }}
        run: exit 1
